<Window x:Class="StudentManagement.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StudentManagement.UI"
        Title="Student Management" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <!-- Column definitions: Menu (250) | Content (*) -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Navigation Panel -->
        <StackPanel Background="#2C3E50" Grid.Column="0" Grid.Row="1" >
            <Grid Width="250" Height="150">
                <!-- Ảnh nền -->
                <Image Source="Assets/Icons/dotted.jpg" Stretch="UniformToFill"/>

                <!-- Overlay nội dung -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
                    <!-- Tên phần mềm -->
                    <Image Source="Assets/Icons/logo.png" VerticalAlignment="Center" Width="200" Height="100"></Image>

                    <!-- Vai trò -->
                    <TextBlock Text="Admin"  
                       FontSize="14" 
                       HorizontalAlignment="Center" 
                       Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>

            <Button Content="Dashboard" Name="BtnDashboard" Style="{StaticResource MenuButtonStyle}" Click="BtnDashboard_Click"/>
            <Button Content="Quản lý Sinh viên" Name="BtnStudents" Style="{StaticResource MenuButtonStyle}" Click="BtnStudents_Click"/>
            <Button Content="Quản lý Giảng viên" Name="BtnTeachers" Style="{StaticResource MenuButtonStyle}" Click="BtnTeachers_Click"/>
            <Button Content="Quản lý Khoa" Name="BtnDepartments" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="Quản lý Lớp" Name="BtnClasses" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="Quản lý Môn" Name="BtnSubjects" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="Tài khoản" Name="BtnAccounts" Style="{StaticResource MenuButtonStyle}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="0" Background="GhostWhite" VerticalAlignment="Top" Margin="0,0,0,5" Height="50">
            <Image Source="Assets/Icons/list.png" Style="{DynamicResource SearchIconStyle}" Width="80" VerticalAlignment="Center"/>
            <TextBox x:Name="SearchBox" Width="480" Margin="0,0,10,0" 
                 FontSize="14" Height="30" Style="{DynamicResource TextBoxStyle}"/>
            <Button Content="Search" Width="120" Height="30" Click="Search_Click" Style="{DynamicResource SearchButtonStyle}"/>
        </StackPanel>
        <!-- Line đứng giữa 2 cột -->
        <Border Grid.Column="0" Grid.Row="0" Width="1" Background="Black" HorizontalAlignment="Right"/>

        <!-- Right Content Area -->
        <ContentControl x:Name="MainContent" Grid.Column="1" Background="#ECF0F1"/>
        
    </Grid>
</Window>
